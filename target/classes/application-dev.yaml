# Dev profile — local development. Overrides ONLY profile-specific keys.
# Base application.yaml (secret ${VAR} placeholders, fail-fast) is unchanged.
# SDD-PROVENANCE: U-005 | vault: .mega-sdd/vaults/api-platform | Spring profile dev
coresystem:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:7001
springdoc:
  swagger-ui:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health
server:
  port: ${SERVER_PORT:7001}   # OQ-AP-2 — dev default 7001 (match prod)
