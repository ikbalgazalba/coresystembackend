---
id: U-010
title: Master operasional entities + controllers E29/E31/E32/E33/E34/E35
vault_source: 03-data-model.md#master-operasional-lain-tier-a; 04-flows.md#f-u-006
depends_on: [U-001, U-009]
target_files:
  - path: src/main/java/com/coresystem/coresystembackend/masterdata/operational/MasterOperationalEntities.java
    operation: create
  - path: src/main/java/com/coresystem/coresystembackend/masterdata/operational/MasterOperationalController.java
    operation: create
  - path: src/main/java/com/coresystem/coresystembackend/masterdata/operational/MasterOperationalService.java
    operation: create
acceptance_test:
  - type: test
    command: ./mvnw test -Dtest=MasterOperationalTest
    expects: passes
superpowers_skills:
  - test-driven-development
binding_refs: [C-05, C-12, C-18, OQ-DLRPTN-04, OQ-BE07-06, OQ-DLRPTN-02, OQ-EXTMASTERS-08]
estimated_complexity: medium
starterkit_context_consumed: false
module: M-default
squad: default
---

# Unit U-010 — Master operasional entities + controllers E29/E31/E32/E33/E34/E35

## Goal

Implement 7 master operasional Tier A entities (`mst_approval_reason`, `mst_credit_source`, `mst_branch_credit_source`, `mst_blacklist_override`, `mst_public_holiday`, `mst_general_parameter`, `mst_promotion_line_text`) + controllers E29/E31-E35. BLACKLIST_OVERRIDE + GENERAL_PARAMETER → maker-checker WAJIB. `is_updateable=false` → `409`. `PUBLIC_HOLIDAY` satu-satunya ber-DELETE (OQ-BE07-06).

## Context

- **Vault source:** `03-data-model.md §Master operasional lain (Tier A)`; `04-flows.md F-U-006` (E29/E31-E35); `constitution §I-003` (no hard delete, except PUBLIC_HOLIDAY), `§I-002` (maker-checker BLACKLIST_OVERRIDE/GENERAL_PARAMETER); `06-constraints.md BR-BE07-05/06/23`
- **Binding refs:** C-05 (7 master operasional NEW), C-12 (E29-E38 — this unit E29/E31-E35), C-18 (no hard delete), OQ-DLRPTN-04 (approval-reason type '9' exposed? — endpoint accepts filter `type`, no hardcode subset), OQ-BE07-06 (PUBLIC_HOLIDAY hard-delete vs deactivate — USULAN DELETE allowed)
- **Why this scope:** BE-07 §10 urutan (5). Master operasional konsumsi 03/05 (reason-code), 01 (blacklist-override reason-gate RFA). `mst_blacklist_override` AML regulated adjacency → maker-checker + append-only audit.

## Implementation steps

1. Entities (bundle in `MasterOperationalEntities.java` or separate files — ≤5 files constraint → group smaller ones): `ApprovalReason` (`mst_approval_reason`, type enum `'1'|'2'|'3'|'9'` — makna OQ-DLRPTN-04), `CreditSource` (`mst_credit_source` LOKAL BR-CREDITSRC-1), `BranchCreditSource` (`mst_branch_credit_source`, `photoRequired`/`printSurveyReport` scoped per branch), `BlacklistOverride` (`mst_blacklist_override` — `nationalId` VARCHAR(16) `[LOCKED]`, `reasonCode` enum 5-reason car, `justification` wajib, `validFrom`/`validUntil`, `isActive`; maker-checker + append-only audit BR-BE07-06), `PublicHoliday` (`mst_public_holiday` — `holidayName`/`holidayDate` NOT NULL, `ux_..._holiday_date`; satu-satunya ber-DELETE OQ-BE07-06), `GeneralParameter` (`mst_general_parameter` — `isUpdateable`/`isVisible`; `is_updateable=false` → `409`), `PromotionLineText` (`mst_promotion_line_text`).
2. `MasterOperationalService` — E29 approval-reason CRUD (type filter, no hardcode subset OQ-DLRPTN-04); E31 credit-source + branch mapping; E32 blacklist-override (maker-checker U-009 + `justification` wajib → `422` if missing, append-only audit); E33 public-holiday CRUD (DELETE allowed — OQ-BE07-06 USULAN); E34 general-parameter (PATCH only; `is_updateable=false` → `409`; no create/delete via API); E35 promotion-line-text.
3. `MasterOperationalController` `@RestController` — E29/E31/E32/E33/E34/E35. Auth: Credit (Admin); E32/E34 maker+checker.
4. `MasterOperationalTest` — AC-14 (DELETE approval-reason → 405/404; deactivate-only, except PUBLIC_HOLIDAY), AC-15 (blacklist-override tanpa justification → 422; dengan → 202 pending_approval; append-only audit), AC-16 (general-parameter `is_updateable=false` → 409).

## Acceptance criteria

- E32 BLACKLIST_OVERRIDE: `justification` wajib (`422` if missing); write → `202 pending_approval` (maker-checker); append-only audit (AC-15, BR-BE07-06).
- E34 `is_updateable=false` → `409` (AC-16, BR-BE07-23); no create/delete via API.
- E33 PUBLIC_HOLIDAY: DELETE allowed (satu-satunya — OQ-BE07-06); others deactivate-only (AC-14, BR-BE07-03).
- E29 approval-reason: `type` filter accepted, no hardcode subset (OQ-DLRPTN-04 — endpoint read menerima filter `type` eksplisit).
- `mst_blacklist_override.nationalId` VARCHAR(16) `[LOCKED]`.
- `mst_credit_source` = LOKAL (BR-CREDITSRC-1).
- `mst_public_holiday.holidayName`/`holidayDate` NOT NULL (reject NULL legacy — DB-CONVENTIONS).
- All tests pass.

## Out of scope

- GL_TRANSACTION_TYPE_LINK (U-012 — maker-checker CoA, separate).
- cfg_number_format (U-012).
- Lookup Tier C E30 (U-011).
- BLACKLIST_OVERRIDE reason-gate enforcement di 01 (BE-01 — 07 only CRUD).
