# Seed PRD — jwt-login

> **Generated by**: `/mega-sdd:generate-intent` (Mode B `--from-prompt`)
> **Date**: 2026-07-22
> **Source**: user brief + explicit OQ list (no interactive Q&A needed — brief was complete across all 10 taxonomy topics under `--auto`)
> **Status**: DRAFT — not stakeholder-signed. Triggers PRD_STATUS=draft downstream.

---

## §brief — User brief (verbatim)

> "buatkan fitur login menggunakan jwt berdasarkan flow /mega-sdd:auto untuk lebih mudahnya ikuti cara pada aplikasi newmojf /home/ikbalgazalba/AI/Project/newmojf/src/main/java/com/bankmega/newmojf/controller/AuthUserController.java, gunakan environment /home/ikbalgazalba/AI/Project/newmojf/src/main/resources/application-test.properties, baca user yang terdaftar pada table /home/ikbalgazalba/AI/Project/newmojf/src/main/java/com/bankmega/newmojf/model/mojf/mojf_users_Model.java"
>
> Elaborated brief (from orchestrate-flow dispatch, captures the resolved intent):
> Buat fitur login menggunakan JWT pada project coresystembackend (Spring Boot 4.x snapshot, Java 21, Maven wrapper). Saat ini project masih skeleton Spring Initializr (hanya spring-boot-starter, belum -web, belum ada HTTP layer). Target project: coresystembackend (CWD, punya pom.xml starterkit). Aplikasi referensi (BUKAN target rebuild — hanya acuan pola implementasi): newmojf. Tujuan: replikasi pola autentikasi JWT newmojf ke coresystembackend.

---

## §qa — Q&A elaboration session

| # | Topic | Question asked? | User answer |
|---|-------|-----------------|-------------|
| 1 | Project shape | Skipped — brief covered | api-only (backend login REST API) — inferred from "fitur login" + newmojf controller pattern (`(brief; inferred from AuthUserController.java @RestController @RequestMapping("/api/auth"))`) |
| 2 | Primary users / personas | Skipped — brief covered | API consumers / client apps of coresystembackend that need authenticated access `(brief inferred)` |
| 3 | Core problem | Skipped — brief covered | coresystembackend needs a JWT login endpoint replicating newmojf's auth pattern `(brief)` |
| 4 | Top 2–3 user flows | Skipped — brief covered | Login (POST /api/auth/dologin) → returns JWT + user data `(brief; AuthUserController.java /dologin)` |
| 5 | Implementation mode | Asked via orchestrate-flow AskUserQuestion | Auth mechanism = **LDAP (replikasi newmojf)**; chain = **Run full** `(Q&A §orchestrate-flow)` |
| 6 | Tech stack constraints | Skipped — brief covered | Spring Boot 4.1.1-SNAPSHOT, Java 21, Maven, jakarta.persistence, Spring Security 7.x `(brief; codebase-map.md §7)` |
| 7 | Regulatory / compliance | Skipped — brief silent | `(unspecified)` — no regulatory regime stated; LDAP/JWT credential handling implies PDP-Indonesia care but not confirmed |
| 8 | Success metrics | Skipped — brief covered | Login endpoint issues a valid JWT for a registered user; token carries uname; subsequent validation works `(brief; AuthUserController.java)` |
| 9 | Out of scope | Skipped — brief silent | `(unspecified)` — user did not state non-goals; inferred: refresh-token rotation, password-change, user CRUD not in newmojf's `/dologin` scope `(brief inferred)` |
| 10 | Anything else | Skipped — brief covered | Three explicit OQs raised in brief (LDAP infra, DB target, secret management) `(brief)` |

---

## A. Product

Fitur login berbasis JWT untuk coresystembackend — replikasi pola autentikasi aplikasi referensi newmojf. Endpoint menerima username + password, autentikasi via LDAP UCS, lalu menerbitkan JWT + lookup data user terdaftar dari tabel users, dan mengembalikan token beserta identitas user. `(brief)`

## B. Target users

- **API consumer / client app** — klien yang memanggil endpoint `/api/auth/dologin` untuk mendapatkan JWT, lalu menggunakannya untuk akses endpoint terproteksi coresystembackend. `(brief inferred; AuthUserController.java)`

## C. Problem & motivation

coresystembackend belum punya layer autentikasi (skeleton Spring Initializr tanpa `-web`/`-security`). Dibutuhkan mekanisme login konsisten dengan pola newmojf (LDAP UCS → JWT) agar klien bisa mendapatkan token akses. `(brief; codebase-map.md §7)`

## D. Goals / success criteria

- Endpoint `POST /api/auth/dologin` menerima `{uname, pass}` dan mengembalikan `{token, id, uname, kodeMitra, role}`. `(brief; AuthUserController.java:128-138)`
- JWT diterbitkan dari uname, secret + expiration dibaca dari config. `(brief; application-test.properties:24-25)`
- User terdaftar dibaca dari tabel users (struktur mojf_users_Model, diadaptasi). `(brief; mojf_users_Model.java)`

## E. In-scope features (v1)

1. **Login (JWT issuance)** — `POST /api/auth/dologin`: autentikasi LDAP UCS → terbitkan JWT → lookup user → return JwtResponse. `(brief; AuthUserController.java:89-150)`

## F. Out of scope (v1)

- Refresh-token rotation `(brief inferred — newmojf /dologin tidak ada refresh)`
- Password change / reset `(brief inferred)`
- User CRUD / registration `(brief inferred)`
- Manajemen sesi berbasis cookie/server-side session (stateless JWT) `(brief inferred; SecurityConfig stateless)`

## G. Constraints

### Technical
- Spring Boot 4.1.1-SNAPSHOT, Java 21, Maven wrapper. `(brief; codebase-map.md §7)`
- `jakarta.persistence` (BUKAN `javax.persistence`) — Boot 4.x mandate; referensi newmojf pakai javax, HARUS diadaptasi. `(brief; codebase-map.md §7 version_caveat)`
- Spring Security 7.x `SecurityFilterChain` bean — `WebSecurityConfigurerAdapter` dilarang. `(brief; codebase-map.md §7; spring.md pack)`
- Constructor injection — field `@Autowired` dilarang. `(brief; spring.md pack)`
- DTO di boundary API — jangan expose entity langsung. `(brief; spring.md pack)`
- Spring Boot snapshot resolve dari `repo.spring.io/snapshot` — butuh akses jaringan. `(brief; codebase-map.md §7)`

### Regulatory & compliance
- `(unspecified)` — tidak ada regime regulasi yang dinyatakan. PDP-Indonesia implisit untuk penanganan kredensial tapi belum dikonfirmasi.

### Business
> Timeline / budget — `(unspecified)`.

## H. Implementation mode

**`new`** (greenfield — coresystembackend skeleton belum punya kode fitur) `(brief; codebase-map.md §1)`

## I. Mentioned but not specified (downstream OQs)

- **LDAP infra** — coresystembackend belum punya endpoint/server LDAP UCS aktif; perlu konfirmasi: pakai LDAP UCS newmojf yang sama, atau perlu mock/fallback auth DB untuk dev? `(brief explicit OQ)`
- **Target database** — sambung ke PostgreSQL newmojf existing (`10.95.1.43:5432/newmojf`) atau DB baru khusus coresystembackend? Tabel `users` apakah sudah berisi data user terdaftar? `(brief explicit OQ)`
- **Secret management** — `jwtSecret` dikelola bagaimana: hardcode seperti referensi (`jwtMojfSecretkey`) vs env var/Vault? `(brief explicit OQ; spring.md pack security idiom: externalize)`
- **Entity/table naming** — pack standard `entity/` + tabel `users`; referensi newmojf pakai `model/` + tabel `mojf_users`. Konvensi yang dipakai? `(brief; spring.md pack vs mojf_users_Model.java)`
- **Compliance regime** — PDP-Indonesia atau lainnya? `(§G unspecified)`
- **LDAP response contract** — struktur `responseCode`/`responseDescription` dari LDAP UCS; kode sukses "00"/"01" sesuai newmojf, perlu konfirmasi endpoint. `(brief; AuthUserController.java:99-102)`
