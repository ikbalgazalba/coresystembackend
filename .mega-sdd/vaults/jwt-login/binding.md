---
vault: .mega-sdd/vaults/jwt-login
codebase_map: .mega-sdd/codebase/codebase-map.md
bound_at: 2026-07-22T00:00:00Z
strict: false
binding_metadata:
  codebase_map_provenance: snapshot-verified
constitution_hash: 6b2ce92610c1e05a28292ad0bc96a59353c7969a29c2dd86ca60322d8c3f3fcb
---

> REGENERATED by bind-codebase on every run — do NOT hand-edit. Manual verdict edits are LOST on re-bind; CONFLICT resolutions belong in `/mega-sdd:resolve-oq --binding` (which records them durably in decisions.md).

# Binding Manifest

## Summary
- claims_total: 14
- confirmed: 0
- conflict: 0
- oq: 14

> **Implementation mode = `new` (greenfield).** coresystembackend is a Spring Initializr skeleton — `codebase-map.md` §2–§4 report "None detected" for public interfaces, routes, and data models (only the `CoresystembackendApplication` bootstrap class exists). There is NO existing feature code for any vault claim to match against. Per implementation-state classification, every claim is `NEW` (no anchor in the codebase) — there is nothing to CONFLICT with. The 0-CONFLICT result here is a *consequence of greenfield emptiness*, not a validation that the design is correct; correctness was established upstream in generate-intent (source-traced to the newmojf reference) + the phase-advisor pass. The binding gate's role here is to confirm no existing code contradicts the new design (true — there is none) and to propagate the 17 vault OQs (14 claim-level bindings + tech-OQ recommendations) into unit-level grounding.

## Confirmed Claims (0)

> None. No vault claim has a matching anchor in the codebase (greenfield skeleton). All claims are `NEW` — they will be *created* by execute-bolts, not verified against existing code. `generate-units` will assign `task_type: create` to every unit derived from these claims.

## Implementation State Map (14; field_diff column when precision_tier: ast)

| Claim ID | Verdict | State | Anchor | Confidence | Field diff |
|---|---|---|---|---|---|
| C-001 | OQ | NEW | — (no `@RestController` in codebase-map §2) | n/a | n/a |
| C-002 | OQ | NEW | — (codebase-map §3 routes: "None detected") | n/a | n/a |
| C-003 | OQ | NEW | — (no `@Entity` in codebase-map §4) | n/a | n/a |
| C-004 | OQ | NEW | — (codebase-map §2: only `CoresystembackendApplication`) | n/a | n/a |
| C-005 | OQ | NEW | — (no `UserRepository` / JPA in codebase-map §2/§7) | n/a | n/a |
| C-006 | OQ | NEW | — (no `JwtUtils` in codebase-map §2) | n/a | n/a |
| C-007 | OQ | NEW | — (no `SecurityConfig` in codebase-map §2/§6 — auth: none) | n/a | n/a |
| C-008 | OQ | NEW | — (no DTO classes in codebase-map §2) | n/a | n/a |
| C-009 | OQ | NEW | — (no `LdapUcsService` in codebase-map §2) | n/a | n/a |
| C-010 | OQ | NEW | — (codebase-map §7: `spring-boot-starter` only; no -web/-security/-data-jpa/postgresql/jjwt) | n/a | n/a |
| C-011 | OQ | NEW | — (codebase-map §7: `application.yaml` only sets `spring.application.name`) | n/a | n/a |
| C-012 | OQ | NEW | — (codebase-map §5/§7: no `controller/`/`entity/`/etc packages — only bootstrap package) | n/a | n/a |
| C-013 | OQ | NEW | — (codebase-map §7: PostgreSQL driver not declared) | n/a | n/a |
| C-014 | OQ | NEW | — (codebase-map §6: no auth pattern; no LDAP integration) | n/a | n/a |

### Claim detail (verbatim from vault)

- **C-001** | `02-architecture.md` §Backend component table | `AuthUserController` `@RestController` `@RequestMapping("/api/auth")` — endpoint `POST /dologin`. *(02-architecture.md: component AuthUserController)*
- **C-002** | `02-architecture.md` §API contracts | `POST /api/auth/dologin` accepts `{uname, pass}`, returns `JwtResponse {token, type, id, uname, mitKode, urole}`. *(02-architecture.md: API contracts)*
- **C-003** | `03-data-model.md` §Entities | Entity `users` (18 fields: id, uname, pass, nama_lengkap, kode_unit_kerja, cakupan, urole, created_date, created_by, last_modified, modified_by, last_login, kode_mitra, active, expired_days, kode_kelas_user, status_user), `@Entity @Table @Id @GeneratedValue(IDENTITY)`, `jakarta.persistence`. *(03-data-model.md: DBML)*
- **C-004** | `02-architecture.md` §Backend | `Users` entity in `entity/` package. *(02-architecture.md: component Users)*
- **C-005** | `02-architecture.md` §Backend | `UserRepository extends JpaRepository<Users, Long>`, `findByUname(String)`. *(02-architecture.md: component UserRepository)*
- **C-006** | `02-architecture.md` §Backend + `04-flows.md` F-S-001 | `JwtUtils.generateTokenFromUname(uname)` + validate/parse; secret+exp from config (`jwtExpirationMs=86400000`). *(02-architecture.md: component JwtUtils)*
- **C-007** | `02-architecture.md` §Backend | `SecurityConfig` Spring Security 7.x `@Bean SecurityFilterChain` — permitAll `/api/auth/**`, stateless, CSRF off, `PasswordEncoder`. *(02-architecture.md: component SecurityConfig)*
- **C-008** | `02-architecture.md` §Backend | DTOs `LoginRequest{uname,pass}`, `JwtResponse{token,type,id,uname,mitKode,urole}`, `MessageResponse{message}`. *(02-architecture.md: component DTO)*
- **C-009** | `02-architecture.md` §Backend + Integrations | `LdapUcsService` replicates `LDAP_UCS_Utils.authLDAPNew(uname,pass)` → `{responseCode, responseDescription}`; success codes "00"/"01". *(02-architecture.md: component LdapUcsService)*
- **C-010** | `06-constraints.md` §Technical | Dependencies to add: `spring-boot-starter-web`, `-security`, `-data-jpa`, `org.postgresql:postgresql`, `io.jsonwebtoken:jjwt-{api,impl,jackson}`. *(06-constraints.md: dependencies)*
- **C-011** | `02-architecture.md` §starterkit binding + `06-constraints.md` | Config in `application.yaml` (YAML): `coresystem.app.jwtSecret` (env-var placeholder), `jwtExpirationMs`, PostgreSQL datasource. *(02-architecture.md: config)*
- **C-012** | `02-architecture.md` §Starterkit binding | Package structure `controller/`, `service/`, `repository/`, `entity/`, `dto/`, `config/` under `com.coresystem.coresystembackend`. *(02-architecture.md: starterkit binding)*
- **C-013** | `02-architecture.md` §Integrations | PostgreSQL datasource (JDBC) for persistence. *(02-architecture.md: Integrations)*
- **C-014** | `02-architecture.md` §Integrations | LDAP UCS outbound integration (sync) for credential auth. *(02-architecture.md: Integrations)*

## Tech-OQ Auto-Resolved (Scan) (0)

> No `resolution_mode: scan` OQs in this vault. All tech OQs use `recommend` mode (design/dependency choices, not deterministic codebase lookups — appropriate for a greenfield skeleton with nothing to scan).

## Tech-OQ Recommendations (review required) (7)

> Confidence gate: ONLY `classification_confidence: high` recommendations are validated for citation resolution here. `medium`/`low` pass through to manual review (listed in 00-index.md Auto-Classification Review). Recommendations do NOT block the pipeline.

### OQ-AR-4 [P2] [tech / recommend] [conf: high] — VALIDATED ✓
- **recommendation**: `io.jsonwebtoken:jjwt-api` + `jjwt-impl` + `jjwt-jackson` versi `0.12.x` (runtime scope). WAJIB rewrite API legacy newmojf ke 0.12.x API.
- **rationale**: newmojf VERIFIED memakai jjwt (`JwtUtils.java:12-17`). Impl pakai API legacy `signWith(SignatureAlgorithm.HS512, jwtSecret)` (`:38,48`) + `Jwts.parser().setSigningKey(...)` (`:53,58`) — dihapus di jjwt 0.11+. Replikasi 0.12.x WAJIB rewrite: `Jwts.builder().signWith(Key)` + `Jwts.parserBuilder().verifyWith(key).build()`.
- **scan_citations**: `JwtUtils.java:12-17,38,48,53,58` (newmojf reference — resolves ✓), `codebase-map.md §7` (Boot 4.x / Java 21 — resolves ✓)
- **fallback_if_wrong**: jika jjwt 0.12.x tidak kompatibel snapshot Boot 4.x, fallback ke `nimbus-jose-jwt` atau Spring Security OAuth2 resource server JWT.
- **Citation resolution**: PASS (citations resolve to newmojf reference file + codebase-map §7). User actions: **ACCEPT** / **OVERRIDE** / **REJECT**.

> The remaining 6 recommendations (OQ-AR-3, OQ-AR-5, OQ-DM-1, OQ-DM-2, OQ-DM-3, OQ-CN-2) are `medium`/`low` confidence — passed through unchanged for manual review per the confidence gate. OQ-DC-1 (retagged to tech/recommend/medium in advisor pass) also passes through. See `00-index.md` §Auto-Classification Review.

## Suggested Unit Hard Rules

> Picked up by generate-units; inserted into each relevant unit's `## Hard rules` (machine-validated) or `## Anti-patterns` (informational).

### Hard rules (machine-validated)

> Sourced from `framework-conventions/spring.md` §Hard Rules emitted + vault `constitution.md`. These are mechanically detectable (ast-grep / naming pattern) and anchored in the codebase-map (pack-resolved §7). Each will be injected into the relevant unit's Hard Rules with `id: constitution-<clause>` / `pack-<rule>`.

| Source | Suggested rule | Applies to units derived from |
|---|---|---|
| spring.md §Hard Rules | Controller classes MUST end with `Controller` suffix (`Controller\.java$`) | C-001 (AuthUserController) |
| spring.md §Hard Rules | Service classes MUST end with `Service` suffix (`Service\.java$`) | C-009 (LdapUcsService) |
| spring.md §Hard Rules | Repository interfaces MUST end with `Repository` suffix + extend Spring Data repo | C-005 (UserRepository) |
| spring.md §Hard Rules | Entity classes MUST carry `@Entity` and `@Id` | C-003, C-004 (Users) |
| spring.md §Hard Rules | Field injection (`@Autowired\s+private`) MUST NOT be used — constructor injection | C-001, C-006, C-007, C-009 (all components) |
| spring.md §Hard Rules | Entity objects MUST NOT be returned from REST controllers — use DTOs | C-001, C-002, C-008 |
| spring.md §Hard Rules | Business logic MUST reside in `@Service` classes, not controllers or repositories | C-001, C-009 (orchestration/JWT assembly → service, not controller) |
| spring.md §Hard Rules | Multi-step write operations MUST be wrapped in a `@Transactional` service method (`@Transactional`) | C-009 (OQ-DM-2 fallback write path), any future write unit |
| constitution §B-001 | `jakarta.persistence.*` wajib; `javax.persistence.*` dilarang (forbidden import pattern) | C-003, C-004 (Users entity) |
| constitution §B-002 | `SecurityFilterChain` bean; `WebSecurityConfigurerAdapter` forbidden (forbidden import/extends pattern) | C-007 (SecurityConfig) |
| constitution §B-005 | `PasswordEncoder` bean MUST be provided in `SecurityConfig` (even though auth via LDAP) — detectable: bean definition presence | C-007 (SecurityConfig) |
| constitution §C-001 | Controllers MUST NOT call other controllers — use Services (detectable: controller injecting/invoking another `*Controller`) | C-001 (AuthUserController) |
| constitution §B-006 | JANGAN replikasi `disableSslVerification()` trust-all (forbidden: `X509TrustManager` empty `checkServerTrusted` + `HostnameVerifier` allHostsValid) | C-009 (LdapUcsService) |
| constitution §B-007 | Error response JANGAN echo raw `e.getMessage()`/internal `responseDescription` verbatim | C-001, C-002, C-009 |

### Anti-patterns (informational)

| Source | Suggested guidance | Applies to units derived from |
|---|---|---|
| spring.md §Forbidden | `WebSecurityConfigurerAdapter` (removed Spring 6) | C-007 |
| spring.md §Forbidden | `System.out.println()` for logging — use SLF4J | all units |
| spring.md §Forbidden | Hardcoded credentials/secrets in source | C-006 (jwtSecret), C-011 (config) |
| constitution §D-001 | JANGAN salin verbatim legacy newmojf conventions (javax, field @Autowired, model/, .properties) — adaptasi ke pack | C-003, C-004, C-011, C-012 |
| constitution §D-002 | JANGAN hardcode `jwtSecret` literal (newmojf `jwtMojfSecretkey` = smell) | C-006, C-011 |

## Conflicts (0) — BLOCKING

> **No conflicts.** Greenfield skeleton — no existing code contradicts any vault claim. (A 0-conflict greenfield bind is expected; the anti-hallucination work happened upstream in generate-intent's source-tracing + advisor pass, not here. bind-codebase's moat on a greenfield project is: confirm nothing existing is contradicted, propagate OQs + Hard Rules downstream.)

## Open Questions (14 claim-level bindings map to 17 vault OQs)

> The 14 NEW claims carry the 17 vault OQs forward into unit-level grounding. 4 are P1 business-blocking (they block *runtime/execute-bolts execution*, NOT the binding gate — binding passes clean). generate-units will tag the affected units so execute-bolts halts if a P1 is unresolved when that unit runs.

| ID | Question | Source | Auto-resolve attempted |
|---|---|---|---|
| OQ-AR-1 | LDAP UCS infra aktif untuk coresystembackend? (P1 business) | 02-architecture.md | **RESOLVED v1.2** — pakai infra existing newmojf (`openapidev2.bankmega.local:15000` UAT) |
| OQ-AR-2 | Target DB: PostgreSQL newmojf existing atau DB baru? (P1 business) | 02-architecture.md | **RESOLVED v1.2** — pakai DB newmojf existing; migrate to new DB post-deploy |
| OQ-AR-3 | jwtSecret: hardcode vs env var? (P2 tech/recommend) | 02-architecture.md | passed-through (medium conf) |
| OQ-AR-4 | lib JWT jjwt 0.12.x + legacy API rewrite? (P2 tech/recommend/high) | 02-architecture.md | VALIDATED (citations resolve) |
| OQ-AR-5 | CORS: wildcard vs SecurityFilterChain config? (P2 tech/recommend) | 02-architecture.md | passed-through (medium conf) |
| OQ-AR-6 | Prasyarat LDAP UCS (AES key, cred, URLs) tersedia? (P1 business) | 02-architecture.md | **RESOLVED v1.2** — tersedia via `DB_Connection.properties` newmojf; externalize secret |
| OQ-AR-7 | Nama field DTO: verbatim mitKode/urole vs rename? (P2 business) | 02-architecture.md | N/A (business/blocking) |
| OQ-DM-1 | Tabel `users` vs `mojf_users`? (P2 tech/recommend) | 03-data-model.md | passed-through (medium conf) |
| OQ-DM-2 | Update last_login saat login? (P3 tech/recommend) | 03-data-model.md | passed-through (low conf) |
| OQ-DM-3 | DDL: ddl-auto=none vs Flyway? (P3 tech/recommend) | 03-data-model.md | passed-through (medium conf) |
| OQ-FL-1 | Kontrak response LDAP UCS valid? (P1 business) | 04-flows.md | **RESOLVED v1.2 (correction)** — endpoint verified; "00"/"01" `[INFERRED]` |
| OQ-FL-2 | Rate-limiting/lockout? (P3 business) | 04-flows.md | N/A (business/blocking) |
| OQ-FL-3 | Sanitasi error body LDAP (info-leakage)? (P2 business) | 04-flows.md | N/A (business/blocking) |
| OQ-CN-1 | Compliance regime (PDP-Indonesia)? (P3 business) | 06-constraints.md | N/A (business/blocking) |
| OQ-CN-2 | Lombok @Slf4j vs SLF4J manual? (P3 tech/recommend) | 06-constraints.md | passed-through (medium conf) |
| OQ-DC-1 | JWT uname-only vs role claim? (P3 tech/recommend) | 05-decisions.md | passed-through (medium conf) |
| OQ-OV-1 | Success criteria kuantitatif? (P3 business) | 01-overview.md | N/A (business/blocking) |

## Auto-Resolved Deferred OQs (0)

> No deferred OQs in this vault (all OQs are `status: pending`, none `defer_to: binding`).

## Phase-advisor pass (binding focus)

Adversarial review ran (mega-sdd:phase-advisor, opus). 3 findings, all materialized:
- **ADV-BIND-001 (med, hard_rule_gap)** — FIXED: added 2 missing pack Hard Rules ("business logic in @Service" §C-003, "@Transactional multi-step" §C-005) to the Suggested Hard Rules table. Previously 6/8 pack rules surfaced; now 8/8.
- **ADV-BIND-002 (low, hard_rule_gap)** — FIXED: added constitution §B-005 (PasswordEncoder bean presence) + §C-001 (no controller-to-controller calls) as machine-detectable Hard Rules. (A-002/A-003/B-004/D-003 remain informational/soft — acceptable gaps.)
- **ADV-BIND-003 (low, state_map_error)** — FIXED: binding.json `summary` now carries `oq_total: 17` + `oq_note` clarifying 14 = claim-level bindings vs 17 vault OQs.

6 categories CLEAN (0-CONFIRMED correct, 0-CONFLICT correct, OQ-AR-4 VALIDATED citation resolves, greenfield reasoning sound — no legacy-convention replicated verbatim into skeleton).
