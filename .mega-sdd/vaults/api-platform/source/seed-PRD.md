# Seed PRD — api-platform

> **Generated by**: `/mega-sdd:generate-intent` (Mode B — from-prompt)
> **Date**: 2026-07-24
> **Source**: user brief + interactive Q&A (4 substance questions, capped at 10)
> **Status**: DRAFT — not stakeholder-signed. Triggers PRD_STATUS=draft downstream.

---

## §brief — User brief (verbatim)

> "buatkan fitur swagger openAPI, kemudian ubah arsitektur aplikasi ini agar berjalan di docker/kontainer, gunakan papeline /mega-sdd"

---

## §qa — Q&A elaboration session

| # | Topic | Question asked? | User answer |
|---|-------|-----------------|-------------|
| 1 | Project shape | Skipped — brief covered (swagger openAPI + docker/kontainer → api-only + containerization) | (skipped) |
| 2 | Primary users / personas | Skipped — cross-cutting infra epic, no end-user persona | (skipped) |
| 3 | Core problem | Skipped — brief covered (no API docs; no container runtime) | (skipped) |
| 4 | Top 2–3 flows | Skipped — brief covered (expose API docs; containerize the app) | (skipped) |
| 5 | Implementation mode | Skipped — brief covered ("ubah arsitektur aplikasi ini" → existing/brownfield) | (skipped) |
| 6 | Tech stack | Skipped — scan-aware (Spring Boot 4.1.1-SNAPSHOT, Java 21, Maven) | (skipped) |
| 7 | Regulatory / compliance | Skipped — §D-002 secret externalization inherited from jwt-login constitution | (skipped) |
| 8 | Success metrics | Asked (via Swagger scope question) | "Dokumentasi API saja" — OpenAPI 3 spec + Swagger UI for existing + future endpoints |
| 9 | Out of scope | Asked (via Docker strategy question) | "Multi-stage + compose" |
| 10 | Anything else | Asked (env & health) | env_file + volume mount (trust store); Actuator + 2 profiles |

**Substance Q&A (the 4 actual decisions captured):**

| Q | Decision | Rationale captured |
|---|----------|--------------------|
| Swagger scope | **Dokumentasi API saja** — springdoc-openapi; expose OpenAPI 3 spec + Swagger UI for POST /api/auth/dologin + DTOs (LoginRequest/JwtResponse/MessageResponse) + future endpoints. UI path `/swagger-ui.html`, spec `/v3/api-docs`. Safe because /api/auth/** is permitAll. | Minimal, covers existing + future endpoints without security-scheme complexity |
| Docker strategy | **Multi-stage + compose** — multi-stage Dockerfile (Maven build → JRE runtime, eclipse-temurin:21-jre); docker-compose.yml for app + optional PostgreSQL; .dockerignore; build via mvnw inside container (reproducible, no host Maven). | Lean image, reproducible, compose for orchestration |
| Env & secrets | **env_file + volume mount** — preserve §D-002 (no secret in image); compose `env_file: .env` (gitignored); .env.example as template; bankmega SSL trust store mounted as volume (not baked-in) → portable. | Portable, §D-002 compliant |
| Health & profiles | **Actuator + 2 profiles** — add spring-boot-starter-actuator; expose /actuator/health (readiness+liveness) for container probes; Spring profiles dev (local) vs prod (container, trust store via env); application-{profile}.yaml. | Container probes need actuator; profile separation dev/prod |

---

## A. Product

Add an **OpenAPI/Swagger documentation layer** and **Docker containerization** to the existing coresystembackend Spring Boot service. The app already exposes `POST /api/auth/dologin` (jwt-login vault, landed); this epic makes its API self-documenting and deployable as a container. *(brief; Q&A Swagger scope)*

## B. Target users

- **API consumers / frontend devs** — read the OpenAPI spec / Swagger UI to integrate against the login endpoint and future endpoints. *(Q&A Swagger scope, inferred)*
- **Platform / DevOps engineer** — builds and runs the container image, wires env + trust store, probes health. *(Q&A Docker/env/health, inferred)*

## C. Problem & motivation

The service has no API documentation layer (no springdoc/swagger dependency) and no container artifacts (no Dockerfile/compose/.dockerignore) — it runs only via host `mvn spring-boot:run` with a host-specific trust store path. This blocks reproducible deployment and API discoverability. *(brief; scan-codebase §7 observations)*

## D. Goals / success criteria

- OpenAPI 3 spec auto-generated from code; Swagger UI reachable at `/swagger-ui.html`. *(Q&A Swagger scope)*
- App builds and runs inside a Docker container (multi-stage, JRE runtime image). *(Q&A Docker strategy)*
- `docker compose up` starts the app with env from `.env` + trust store mounted as volume. *(Q&A env & secrets)*
- `/actuator/health` reachable for container readiness/liveness probes. *(Q&A health & profiles)*
- No secret baked into the image (§D-002 preserved). *(Q&A env & secrets; jwt-login constitution §D-002)*

## E. In-scope features (v1)

1. **OpenAPI/Swagger documentation** — add springdoc-openapi dependency; auto-generate spec from `AuthUserController` + DTOs; expose `/swagger-ui.html` + `/v3/api-docs`; configure path + title. *(Q&A Swagger scope)*
2. **Dockerfile (multi-stage)** — builder stage runs `./mvnw clean package` (no host Maven); runtime stage `eclipse-temurin:21-jre` runs the JAR; EXPOSE port. *(Q&A Docker strategy)*
3. **docker-compose.yml** — app service (build from Dockerfile) + optional PostgreSQL service; `env_file: .env`; trust store volume mount; healthcheck via actuator. *(Q&A Docker strategy, env & secrets)*
4. **.dockerignore** — exclude target/, .git, .mega-sdd/, .env, etc. *(Q&A Docker strategy)*
5. **Actuator health** — add spring-boot-starter-actuator; expose `/actuator/health` (liveness+readiness); permit the path in SecurityConfig. *(Q&A health & profiles)*
6. **Spring profiles** — `application-dev.yaml` (local) vs `application-prod.yaml` (container, trust store path via env); default profile selection. *(Q&A health & profiles)*

## F. Out of scope (v1)

- Security scheme (Bearer JWT) in the OpenAPI spec — deferred (could be a follow-up; current /api/auth/** is permitAll anyway). *(Q&A Swagger scope — chose docs-only)*
- Distroless/non-root prod image, separate Dockerfile.dev/prod — deferred. *(Q&A Docker strategy — chose multi-stage + compose, not full profiles)*
- Docker secrets / external secret manager — deferred (env_file is v1). *(Q&A env & secrets)*
- CI/CD pipeline (GitHub Actions etc.) — not requested; this epic is local Docker only. *(brief — "gunakan pipeline /mega-sdd" refers to the SDD pipeline, not CI/CD)*
- Fixing the pre-existing jwt-login gaps (bearer filter absent, role not enforced, no @Valid) — out of scope; surfaced as OQs only. *(scan-codebase known_gaps)*

## G. Constraints

### Technical
- Spring Boot 4.1.1-SNAPSHOT, Java 21, Maven (mvnw wrapper committed). *(scan-codebase §7)*
- jakarta.* namespace (Boot 4.x); Spring Security 7.x. *(jwt-login constitution §B-001/§B-002)*
- Existing `application.yaml` uses `${VAR}` placeholders with NO defaults (fail-fast); must not regress. *(scan-codebase §6)*
- springdoc-openapi version must be compatible with Spring Boot 4.x (Boot 4 is snapshot — version compat is an OQ). *(scan-codebase §7 snapshot_repo caveat)*

### Regulatory & compliance
- §D-002: no secret in image/env-literals; env_file (gitignored) only. *(jwt-login constitution §D-002; Q&A env & secrets)*
- §B-006: no trust-all SSL bypass; the bankmega trust store is mounted, never replaced with an empty trust manager. *(jwt-login constitution §B-006)*

### Business
> Timeline / budget — `(unspecified)`

## H. Implementation mode

**`existing`** — brownfield; the app is feature-bearing (jwt-login landed). This epic adds cross-cutting infra to the existing codebase. *(brief "ubah arsitektur aplikasi ini"; scan-codebase mode=existing)*

## I. Mentioned but not specified (downstream OQs)

- springdoc-openapi version compatible with Spring Boot 4.1.1-SNAPSHOT — `(unspecified)` → OQ (tech, scan/recommend). *(scan-codebase §7 snapshot caveat)*
- Container port (8080 default? override?) — `(unspecified)` → OQ. *(Q&A — port not pinned)*
- Trust store path inside the container (mount target) + whether run-app.sh's host path logic is replaced or kept for local — `(unspecified)` → OQ. *(scan-codebase §6 trust store non-portable)*
- PostgreSQL in compose: use a real DB service or only env wiring to the existing newmojf UAT (10.95.1.43)? — `(unspecified)` → OQ. *(scan-codebase §4 datasource URL)*
- Whether Swagger UI should be disabled in prod profile (security exposure of endpoint docs) — `(unspecified)` → OQ. *(Q&A Swagger scope — prod gating not decided)*
- JVM memory/resource limits for the container — `(unspecified)` → OQ. *(brief silent)*
