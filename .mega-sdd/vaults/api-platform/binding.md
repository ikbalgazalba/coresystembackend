---
vault: .mega-sdd/vaults/api-platform
codebase_map: .mega-sdd/codebase/codebase-map.md
bound_at: 2026-07-24T04:20:00Z
strict: false
binding_metadata:
  codebase_map_provenance: snapshot-verified
constitution_hash: 243c371d17869bbe9edc616dd23510f3072f90f64d17ece4d75f3543845bc09c
---

> REGENERATED by bind-codebase on every run — do NOT hand-edit. Manual verdict edits are LOST on re-bind; CONFLICT resolutions belong in `/mega-sdd:resolve-oq --binding` (which records them durably in decisions.md).

# Binding Manifest

## Summary
- claims_total: 16
- confirmed: 7
- conflict: 0
- oq: 9
- (advisor binding pass: 3 low findings materialized — APB-001 jwtExpirationMs default, APB-002 column count 20→17, APB-003 CORS externalization promoted to hard rule + OQ-AP-9)

> **Implementation mode = `existing` (brownfield, feature-bearing).** The api-platform vault adds a cross-cutting OpenAPI/Swagger + Docker layer to the already-shipped jwt-login service. The codebase-map (HEAD 0682bb5, snapshot-verified) confirms: the existing endpoint (`POST /api/auth/dologin`), DTOs, entity, SecurityConfig, and tech-stack constraints are all present and match the vault's "document, don't modify" claims → CONFIRMED/IMPLEMENTED. The NEW components (Dockerfile, docker-compose, .dockerignore, OpenApiConfig, profile YAMLs, springdoc+actuator deps) are genuinely absent from the codebase → NEW (no anchor, no conflict — they will be *created*). The 0-CONFLICT result here is a consequence of the vault correctly describing the existing code as-is and proposing only additive/new artifacts; correctness of the new design was established upstream in generate-intent (source-traced to the brief + Q&A + scan) + the intent phase-advisor pass. The gate's role: confirm no existing code contradicts the new design (true — none does).

## Confirmed Claims (7)

- C-001 | `02-architecture.md` §API documentation layer | codebase-map §3 Routes | `POST /api/auth/dologin` exists and will be auto-documented (no modification) — `AuthUserController.java:54-55`
- C-002 | `02-architecture.md` §API documentation layer | codebase-map §2 Public interfaces | DTOs `LoginRequest`/`JwtResponse`/`MessageResponse` exist and will be auto-documented (no modification)
- C-003 | `03-data-model.md` | codebase-map §4 Data models | `Users` entity (`@Table mojf_users`) exists; correctly NOT exposed in REST (jwt-login §C-002); not modified by this epic
- C-004 | `02-architecture.md` §Containerization layer | codebase-map §2 | `SecurityConfig` exists (`SecurityConfig.java:42`); vault's EDIT is additive-only (new permitAll matchers) — consistent with HR-5
- C-005 | `06-constraints.md` Technical | codebase-map §7 Framework | Spring Boot 4.1.1-SNAPSHOT, Java 21, Maven (mvnw committed), jakarta.* namespace — all confirmed
- C-006 | `06-constraints.md` Technical | codebase-map §6 Pattern signatures | env-placeholder `${VAR}` with NO defaults (fail-fast) preserved — confirmed
- C-007 | `02-architecture.md` §API documentation layer | codebase-map §7 (springdoc absent) + §6 (secrets §D-002) | springdoc-openapi + actuator are ABSENT (correctly to-be-added); §D-002 secret externalization is in force

## Implementation State Map (16; precision_tier: ast)

| Claim ID | Verdict | State | Anchor | Confidence | Field diff |
|---|---|---|---|---|---|
| C-001 | CONFIRMED | IMPLEMENTED | AuthUserController.java:54 + codebase-map §3 | high | (exact match — POST /api/auth/dologin) |
| C-002 | CONFIRMED | IMPLEMENTED | dto/*.java:4 + codebase-map §2 | high | (exact match — 3 DTOs, field-sets match) |
| C-003 | CONFIRMED | IMPLEMENTED | entity/Users.java:13-15 + codebase-map §4 | high | (exact match — mojf_users, 17 @Column fields; corrected from map's prior "20" — advisor APB-002) |
| C-004 | CONFIRMED | IMPLEMENTED | config/SecurityConfig.java:42 + codebase-map §2 | high | (exact match; EDIT is additive) |
| C-005 | CONFIRMED | IMPLEMENTED | pom.xml + codebase-map §7 | high | (exact match — Boot 4.1.1-SNAPSHOT, Java 21, Maven) |
| C-006 | CONFIRMED | IMPLEMENTED | application.yaml + codebase-map §6 | high | (match with caveat — secret/credential `${VAR}` placeholders have NO defaults (fail-fast); `jwtExpirationMs: 86400000` is a literal default (non-secret) — advisor APB-001 narrowed the claim) |
| C-007 | CONFIRMED | NEW | pom.xml (absent deps) + codebase-map §7 | high | (deps absent — to be added) |
| C-008 | OQ | NEW | — | n/a | n/a | OpenApiConfig.java (NEW file, no anchor) |
| C-009 | OQ | NEW | — | n/a | n/a | springdoc-openapi version (OQ-AP-1 — Boot 4 compat unresolved) |
| C-010 | OQ | NEW | — | n/a | n/a | Dockerfile (NEW, no anchor) |
| C-011 | OQ | NEW | — | n/a | n/a | docker-compose.yml (NEW, no anchor) |
| C-012 | OQ | NEW | — | n/a | n/a | .dockerignore (NEW, no anchor) |
| C-013 | OQ | NEW | — | n/a | n/a | application-dev.yaml / application-prod.yaml (NEW, no anchor) |
| C-014 | OQ | NEW | — | n/a | n/a | /v3/api-docs + /swagger-ui.html paths (NEW, not in codebase-map §3) |
| C-015 | OQ | NEW | — | n/a | n/a | /actuator/health path (NEW, actuator absent) |
| C-016 | OQ | NEW | — | n/a | n/a | trust-store mount target path inside container (OQ-AP-3 — human decision) |

## Tech-OQ Auto-Resolved (Scan) (0)

> No tech-OQ auto-resolved at binding. The scan-mode OQs (OQ-AP-8 actuator exposure surface) require a design decision beyond a pure scan match; deferred to human/recommendation. None of the vault's tech OQs had `classification_confidence: high` with a single unambiguous codebase-map match (OQ-AP-5 is `recommend` high-conf but a recommendation, not a scan-resolvable fact).

## Tech-OQ Recommendations (review required) (4)

> Each has ACCEPT / OVERRIDE / REJECT options. Recommendations do NOT block; user reviews one-pass after binding. These carry forward from generate-intent's OQ classification (materialized through the intent phase-advisor).

### OQ-AP-1 [P1] [tech / recommend] [conf: medium]
- Question: springdoc-openapi version compatible with Spring Boot 4.1.1-SNAPSHOT
- Recommendation: *(none yet — needs a Boot-4-compatible springdoc milestone/snapshot lookup; the vault deliberately does NOT invent a version)*
- Fallback if wrong: blocking — request PO/architect to pin a Boot-4-compatible springdoc version
- Citations: codebase-map §7 snapshot_repo caveat

### OQ-AP-2 [P2] [tech / recommend] [conf: medium]
- Question: Container port (default 8080 vs override) + SERVER_PORT wiring
- Recommendation: *(default 8080 — Spring Boot default; application.yaml sets no server.port)*
- Fallback if wrong: default 8080
- Citations: application.yaml (no server.port key) → Spring defaults to 8080

### OQ-AP-4 [P2] [tech / recommend] [conf: medium] *(reclassified from blocking by intent-advisor)*
- Question: PostgreSQL in compose — real DB service vs env-only wiring to existing newmojf UAT
- Recommendation: env-only wiring to existing newmojf UAT (no postgres service in compose) — app already connects to 10.95.1.43 via SPRING_DATASOURCE_URL
- Fallback if wrong: env-only wiring to existing newmojf UAT (no postgres service in compose)
- Citations: codebase-map §4 (datasource URL 10.95.1.43), .env.example:7

### OQ-AP-5 [P2] [tech / recommend] [conf: high]
- Question: Swagger UI prod gating (disable in prod profile? security exposure of docs)
- Recommendation: disable Swagger UI in prod profile via `springdoc.swagger-ui.enabled=false`; keep spec at /v3/api-docs internal
- Fallback if wrong: blocking — confirm prod docs exposure policy with PO
- Citations: codebase-map §2 (LoginRequest.pass documented)

## Suggested Unit Hard Rules

### Hard rules (machine-validated)
| Source | Suggested rule | Applies to units derived from |
|---|---|---|
| constitution §G-001/§D-004 + HR-1/HR-2 | Image must contain no .env/secret/trust-store; runtime stage `eclipse-temurin:21-jre`; builder uses `./mvnw` | Dockerfile unit, compose unit |
| constitution §G-003/§D-002 + HR-1 | compose `app` uses `env_file: .env` + trust-store volume mount; NO `environment:` secret literals | compose unit |
| constitution §G-006 + HR-4 | profile YAMLs override ONLY profile-specific keys; base application.yaml fail-fast behavior preserved | profile-config unit |
| constitution §G-007 + HR-5 | existing `POST /api/auth/dologin` + authz rules NOT weakened; only ADDITIVE permitAll for doc+health paths | SecurityConfig-edit unit, OpenApiConfig unit |
| constitution §B-008 + HR-3 | `/actuator/health/**` permitAll; no other actuator endpoint exposed | SecurityConfig-edit unit |
| constitution §D-003 + HR-7 | springdoc + actuator are the ONLY new deps; no Lombok/extra libs | pom.xml-edit unit |
| advisor binding APB-003 + constitution §G-007 | CORS allowed origins MUST be externalized to `${CORS_ALLOWED_ORIGINS}` (profile-driven) — the existing hardcoded `localhost:3000/8080` (`SecurityConfig.java:89`) breaks browser clients in a container/prod context | SecurityConfig-edit unit, profile-config unit |

### Anti-patterns (informational)
| Source | Suggested guidance | Applies to units derived from |
|---|---|---|
| constitution §B-006 | Do NOT replace trust store with trust-all X509TrustManager; mount the bankmega trust store | compose unit, profile-config unit |
| advisor (intent) | JRE image lacks curl — use Java-based/CMD-SHELL probe or install a client (note choice in Dockerfile) | Dockerfile unit, compose unit |

## Conflicts (0) — BLOCKING

> None. No vault claim contradicts the codebase-map. Existing-code claims (endpoint, DTOs, entity, SecurityConfig, tech stack, env-placeholder pattern) all CONFIRMED/IMPLEMENTED. New-artifact claims (Dockerfile, compose, .dockerignore, OpenApiConfig, profile YAMLs, new deps, new paths) are genuinely absent → NEW, not conflicts.

## Open Questions (10)

- OQ-AP-1 [P1 tech/recommend] springdoc-openapi version for Boot 4.1.1-SNAPSHOT (medium conf — NOT high, so not auto-resolved; carry to resolve-oq)
- OQ-AP-2 [P2 tech/recommend] container port + SERVER_PORT
- OQ-AP-3 [P1 tech/blocking] trust-store mount target path inside container (BLOCKING — human decision, no safe default; resolve-oq required)
- OQ-AP-4 [P2 tech/recommend] PostgreSQL in compose (env-only recommended)
- OQ-AP-5 [P2 tech/recommend] Swagger UI prod gating
- OQ-AP-6 [P3 tech/recommend] JVM memory/CPU limits
- OQ-AP-7 [P3 business/recommend] success metrics (functional DoD fallback)
- OQ-AP-8 [P3 tech/scan] actuator exposure surface
- OQ-AP-9 [P2 tech/recommend] CORS origins externalization — SecurityConfig.java:89 hardcodes `localhost:3000/8080`; container/prod deployment MUST override via `${CORS_ALLOWED_ORIGINS}` placeholder (advisor APB-003 promoted from guidance to OQ + hard rule)
- C-008..C-016 [NEW] — the 9 NEW-component claims above (no anchor; will be created by execute-bolts, `task_type: create`)

## Auto-Resolved Deferred OQs (0)

> None deferred at this vault's generation.

## Decision gate result

`conflict == 0` AND `--strict` NOT set → **CLEAN BINDING.** `<vault>/bound/` produced. Pipeline proceeds to `generate-units`.

> Note: OQ-AP-3 is a P1 `blocking` tech OQ. It does NOT block binding (binding blocks only on CONFLICT, or on OQ only under `--strict`). It WILL surface in `resolve-oq` / `generate-units` as a unit-level grounding decision. The chain continues to generate-units; the trust-store path decision is resolved interactively at the bolt that needs it (the Dockerfile/compose unit), per the convergence/propose-and-confirm bridge.
